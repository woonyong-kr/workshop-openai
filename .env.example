# Background color (RGB values: 0-255)
BG_R=100
BG_G=149
BG_B=237

# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/mydb

# Server port (macOS는 5000이 AirPlay에 점유되므로 5001 권장)
PORT=5001
