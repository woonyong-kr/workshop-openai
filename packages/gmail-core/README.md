# Gmail Core

Reserved workstream for:

- Gmail scan batching
- sender normalization
- classification heuristics
- unsubscribe parsing and execution planning

Port logic from `skills/gmail-mailbox-cleanup` instead of rewriting rules from memory.
